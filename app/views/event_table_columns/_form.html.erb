<%= form_for(@event_table_column) do |f| %>
  <% if @event_table_column.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_table_column.errors.count, "error") %> prohibited this event_table_column from being saved:</h2>

      <ul>
      <% @event_table_column.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
