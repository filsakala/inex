<%= render 'header' %>

<div class="ui padded stackable grid">
  <div class="ui two column row">
    <div class="four wide column">
      <%= render 'panel' %>
    </div>
    <div class="twelve wide column">
      <h2 class="ui header">
        <i class="add circle icon"></i>
        <div class="content">
          <%= t :new_task %>
          <div class="sub header"><%= t :new_task_description %></div>
        </div>
      </h2>

      <%= render 'form' %>
    </div>
  </div>
</div>